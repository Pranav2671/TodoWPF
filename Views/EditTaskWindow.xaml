<Window x:Class="TodoWPF.Views.EditTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Task"
        Height="250"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FAFAFA">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Task Title:" FontWeight="Bold" />
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Task ID:" FontWeight="Bold"/>
            <TextBlock Text="{Binding Id}" Margin="0,5,0,10"/>

            <TextBlock Text="Created At:" FontWeight="Bold"/>
            <TextBlock Text="{Binding CreatedAt}" Margin="0,5,0,20"/>

            <Button Content="Save Changes"
                    Width="120"
                    HorizontalAlignment="Right"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
